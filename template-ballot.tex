\documentclass[14pt,onesided]{letter}
\usepackage[a6paper]{geometry}% http://ctan.org/pkg/geometry
\usepackage{latexsym}
\usepackage[none]{hyphenat}

\pagenumbering{gobble}
\def\option{\item[$\vcenter{\hbox{\Huge$\Box$}}$]}

\begin{document}
    \begin{center}
        \Huge{\VAR{org}}
        \LARGE{\VAR{date}}


        \Large{\textbf{\VAR{title}}}
    \end{center}

    Mark the options  numerically in order of preference. 

    Start from ``1" as your preferred choice and use ascending integers.

    Rank as many options as you wish.

    \begin{itemize}
        \BLOCK{ for candidate in candidate_list }
            \option \VAR{ candidate }
        \BLOCK{ endfor }
    \end{itemize}
\end{document}
